export fn <%= @type.name %>(env: beam.env, argc: c_int, args: [*c] const e.ErlNifTerm) e.ErlNifTerm {
    beam.context = .<%= context(@concurrency) %>;
    return nif.<%= @type.name %>(env, argc, @ptrCast([*] const beam.term, args)).v;
}