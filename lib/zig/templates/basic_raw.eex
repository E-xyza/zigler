export fn <%= Function.nif_alias_for(assigns) %>(env: beam.env, argc: c_int, args: [*c] const e.ErlNifTerm) e.ErlNifTerm {
    return nif.<%= @name %>(env, argc, @ptrCast([*] const beam.term, args)).v;
}